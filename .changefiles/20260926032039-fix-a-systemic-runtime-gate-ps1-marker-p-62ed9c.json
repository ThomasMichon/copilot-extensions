{
  "comment": "Fix a systemic runtime-gate.ps1 marker-placement bug: the allow legacy-compatibility-root marker was on the closing brace of an if/else expression, not the flagged Join-Path line, so it never actually suppressed the finding (same bug already fixed in agent-vault and agent-logger)",
  "changes": [
    {
      "plugin": "agent-bridge",
      "type": "patch"
    },
    {
      "plugin": "agent-codespaces",
      "type": "patch"
    },
    {
      "plugin": "agent-containers",
      "type": "patch"
    },
    {
      "plugin": "agent-mcp",
      "type": "patch"
    }
  ]
}
