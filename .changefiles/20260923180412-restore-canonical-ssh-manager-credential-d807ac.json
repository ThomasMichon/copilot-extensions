{
  "comment": "Restore canonical ssh-manager/credential-relay to match vendored copies (fixes #3361) -- no functional payload change, canonical-only sync",
  "changes": [
    {
      "plugin": "agent-bridge",
      "type": "patch"
    },
    {
      "plugin": "agent-containers",
      "type": "patch"
    },
    {
      "plugin": "agent-mcp",
      "type": "patch"
    }
  ]
}
