{
  "comment": "copilot --detach --ref-file: copy operator reference files to the SSH target outside the checkout and name them to the worker",
  "changes": [
    {
      "plugin": "agent-ssh",
      "type": "patch"
    }
  ]
}
