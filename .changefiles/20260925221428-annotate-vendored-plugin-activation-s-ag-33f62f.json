{
  "comment": "Annotate vendored plugin-activation's agent-worktrees registry-root read with a marketplace-isolation allow marker (legacy-default registry, already intentional).",
  "changes": [
    {
      "plugin": "agent-machines",
      "type": "dev"
    }
  ]
}
