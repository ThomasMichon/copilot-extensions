#!/usr/bin/env bash
# Generated by libs/payload-invocation/generate.py. Do not edit.
# payload-command-catalog-contract: @@CATALOG_CONTRACT_JSON@@
# payload-command-catalog-sha256: @@CATALOG_FILE_SHA256@@
set -eu

json_escape() {
    local value="$1"
    value="${value//\\/\\\\}"
    value="${value//\"/\\\"}"
    value="${value//$'\r'/\\r}"
    value="${value//$'\n'/\\n}"
    value="${value//$'\t'/\\t}"
    printf '%s' "$value"
}

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd -P)"
self_root="$(cd "$script_dir/.." && pwd -P)"
context_root="${COPILOT_PLUGIN_ROOT:-$self_root}"
context_root="$(cd "$context_root" 2>/dev/null && pwd -P)" || {
    printf '%s\n' '{}'
    exit 0
}
[ "$context_root" = "$self_root" ] || {
    printf '%s\n' '{}'
    exit 0
}

command_path="$self_root/@@OUTPUT_DIR@@/@@COMMAND@@"
installer_path="$self_root/scripts/@@INSTALLER@@.sh"
availability="unavailable"
[ -x "$command_path" ] && [ -f "$installer_path" ] && availability="ready"

catalog="{\"schema\":\"copilot-extensions.session-command-catalog\",\"version\":1,\"plugin\":\"@@PLUGIN@@\",\"payload\":{\"provenance\":\"payload-local\"},\"commands\":[{\"id\":\"@@COMMAND@@\",\"argv\":[\"$(json_escape "$command_path")\"],\"shell\":\"direct\",\"purpose\":\"@@PURPOSE@@\",\"availability\":\"$availability\"}]}"
context="## @@PLUGIN@@ session command catalog

Invoke the exact \`argv\` below. Do not search \`PATH\` or substitute a same-named command from another payload.

\`\`\`json
$catalog
\`\`\`"
printf '{"additionalContext":"%s"}\n' "$(json_escape "$context")"
