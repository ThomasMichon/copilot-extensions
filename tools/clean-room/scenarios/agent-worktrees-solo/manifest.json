{
  "name": "agent-worktrees-solo",
  "description": "Tier-P F1 solo scenario: install ONLY agent-worktrees on a fresh box and assert the worktree BASE stands up self-sufficiently -- runtime provisions on first session, binstub on PATH reports a REAL (non-empty) version, read verbs (repos/projects/list) enumerate, and a worktree ROUND-TRIPS (register -> create -> finalize). The anchor of the P1 solo set (the base other plugins degrade against). Name-free / public.",
  "tier": "P",
  "family": "F1",
  "image": "base",
  "prereqs": { "present": ["git", "python3", "node", "uv"], "provisioned_by_flow": [] },
  "auth": { "copilot": "required", "gh": "optional", "ado": "none" },
  "config": {
    "CR_MARKETPLACE_REPO": "ThomasMichon/copilot-extensions",
    "CR_MARKETPLACE_NAME": "copilot-extensions",
    "CR_UV_INDEX": "(opt-in uv-index fixture)"
  },
  "expected_artifacts": [
    "~/.copilot/installed-plugins/<marketplace>/agent-worktrees",
    "~/.agent-worktrees/versions/<ver> (runtime) + ~/.agent-worktrees/current-version",
    "~/.local/bin/agent-worktrees (binstub)",
    "~/.agent-worktrees/projects.yaml (after register)"
  ],
  "stages": [
    { "n": 0, "title": "environment (fresh machine)" },
    { "n": 1, "title": "install ONLY agent-worktrees" },
    { "n": 2, "title": "runtime provisions on first session (venv + binstub)" },
    { "n": 3, "title": "binstub on PATH + reports a REAL version" },
    { "n": 4, "title": "read verbs enumerate (repos/projects/list)" },
    { "n": 5, "title": "worktree round-trips (register -> create -> finalize)" }
  ]
}
