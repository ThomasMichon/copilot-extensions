{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "type": "command",
        "powershell": "$s = \"$env:USERPROFILE\\.copilot\\installed-plugins\\copilot-extensions\\agent-ssh\\scripts\\bootstrap-check.ps1\"; if (-not (Test-Path $s)) { $s = \"$env:USERPROFILE\\.agent-ssh\\bin\\bootstrap-check.ps1\" }; if (Test-Path $s) { $p = Get-Command pwsh -ErrorAction SilentlyContinue; if ($p) { & $p.Source -NoProfile -File $s } else { powershell.exe -NoProfile -ExecutionPolicy Bypass -File $s } }",
        "bash": "s=\"$HOME/.agent-ssh/bin/bootstrap-check.sh\"; [ -f \"$s\" ] && bash \"$s\"",
        "timeoutSec": 15
      }
    ]
  }
}
