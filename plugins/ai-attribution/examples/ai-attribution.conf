# Operator config example.
# Place at ~/.copilot/ai-attribution.conf, the platform config home, or expose
# it as ai-attribution.conf under a trusted COPILOT_CUSTOM_INSTRUCTIONS_DIRS
# entry outside the target repository.

disclosure=always
owned_account=github.com/example-owner
