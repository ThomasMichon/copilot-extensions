{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "type": "command",
        "powershell": "$s = \"$env:USERPROFILE\\.copilot\\installed-plugins\\copilot-extensions\\ai-attribution\\scripts\\emit-policy.ps1\"; if (Test-Path -LiteralPath $s) { $p = Get-Command pwsh -ErrorAction SilentlyContinue; if ($p) { & $p.Source -NoProfile -File $s } else { powershell.exe -NoProfile -ExecutionPolicy Bypass -File $s } } else { [Console]::Error.WriteLine('[ai-attribution] hook script is missing; no policy context emitted'); [Console]::Out.Write('{}') }",
        "bash": "s=\"$HOME/.copilot/installed-plugins/copilot-extensions/ai-attribution/scripts/emit-policy.sh\"; if [ -f \"$s\" ]; then bash \"$s\"; else printf '%s\\n' '[ai-attribution] hook script is missing; no policy context emitted' >&2; printf '{}'; fi",
        "timeoutSec": 10
      }
    ]
  }
}
