{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "type": "command",
        "powershell": "$root = $env:COPILOT_PLUGIN_ROOT; if (-not $root) { $root = $env:PLUGIN_ROOT }; if (-not $root) { $root = $env:CLAUDE_PLUGIN_ROOT }; if ($root) { $s = Join-Path (Join-Path $root 'scripts') 'emit-guidance.ps1'; if (Test-Path -LiteralPath $s -PathType Leaf) { & $s } else { '{}' } } else { '{}' }",
        "bash": "root=\"${COPILOT_PLUGIN_ROOT:-${PLUGIN_ROOT:-${CLAUDE_PLUGIN_ROOT:-}}}\"; if [ -n \"$root\" ]; then s=\"$root/scripts/emit-guidance.sh\"; if [ -f \"$s\" ]; then bash \"$s\"; else printf '{}'; fi; else printf '{}'; fi",
        "timeoutSec": 5
      }
    ]
  }
}
