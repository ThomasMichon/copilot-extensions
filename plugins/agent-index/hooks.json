{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "type": "command",
        "powershell": "$r = $env:COPILOT_PLUGIN_ROOT; if (-not $r) { $r = (Get-Location).Path }; $s = (Join-Path $r 'scripts\\bootstrap-check.ps1'); if (Test-Path $s) { & $s }",
        "bash": "r=\"${COPILOT_PLUGIN_ROOT:-$PWD}\"; s=\"$r/scripts/bootstrap-check.sh\"; [ -f \"$s\" ] && bash \"$s\"",
        "timeoutSec": 30
      },
      {
        "type": "command",
        "powershell": "$r = $env:COPILOT_PLUGIN_ROOT; if (-not $r) { $r = (Get-Location).Path }; $s = (Join-Path $r 'scripts\\ensure-service.ps1'); if (Test-Path $s) { & $s }",
        "bash": "r=\"${COPILOT_PLUGIN_ROOT:-$PWD}\"; s=\"$r/scripts/ensure-service.sh\"; [ -f \"$s\" ] && bash \"$s\"",
        "timeoutSec": 20
      },
      {
        "type": "command",
        "powershell": "$r = $env:COPILOT_PLUGIN_ROOT; if (-not $r) { $r = $env:PLUGIN_ROOT }; if (-not $r) { $r = $env:CLAUDE_PLUGIN_ROOT }; if ($r) { try { [IO.Directory]::SetCurrentDirectory($env:USERPROFILE) } catch {}; $w = Join-Path (Join-Path $r 'scripts') 'invoke-context-contributor.ps1'; if (Test-Path -LiteralPath $w -PathType Leaf) { & $w 'agent-index@copilot-extensions' 'command-catalog' 'scripts/emit-command-catalog.ps1' } else { [Console]::Out.Write('{}') } } else { [Console]::Out.Write('{}') }",
        "bash": "r=\"${COPILOT_PLUGIN_ROOT:-${PLUGIN_ROOT:-${CLAUDE_PLUGIN_ROOT:-}}}\"; w=\"$r/scripts/invoke-context-contributor.sh\"; if [ -n \"$r\" ] && [ -f \"$w\" ]; then bash \"$w\" agent-index@copilot-extensions command-catalog scripts/emit-command-catalog.sh; else printf '{}'; fi",
        "timeoutSec": 30
      },
      {
        "type": "command",
        "powershell": "$r = $env:COPILOT_PLUGIN_ROOT; if (-not $r) { $r = $env:PLUGIN_ROOT }; if (-not $r) { $r = $env:CLAUDE_PLUGIN_ROOT }; if ($r) { try { [IO.Directory]::SetCurrentDirectory($env:USERPROFILE) } catch {}; $w = Join-Path (Join-Path $r 'scripts') 'invoke-context-contributor.ps1'; if (Test-Path -LiteralPath $w -PathType Leaf) { & $w 'agent-index@copilot-extensions' 'scope-binding' 'scripts/emit-scope-binding.ps1' } else { [Console]::Out.Write('{}') } } else { [Console]::Out.Write('{}') }",
        "bash": "r=\"${COPILOT_PLUGIN_ROOT:-${PLUGIN_ROOT:-${CLAUDE_PLUGIN_ROOT:-}}}\"; w=\"$r/scripts/invoke-context-contributor.sh\"; if [ -n \"$r\" ] && [ -f \"$w\" ]; then bash \"$w\" agent-index@copilot-extensions scope-binding scripts/emit-scope-binding.sh; else printf '{}'; fi",
        "timeoutSec": 30
      }
    ]
  }
}
