{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "type": "command",
        "powershell": "$s = Join-Path $env:COPILOT_PLUGIN_ROOT 'scripts\\emit-contribution-boundary.ps1'; if (Test-Path $s) { & $s } else { '{}' }",
        "bash": "s=\"$COPILOT_PLUGIN_ROOT/scripts/emit-contribution-boundary.sh\"; if [ -f \"$s\" ]; then bash \"$s\"; else printf '{}'; fi",
        "timeoutSec": 10
      }
    ]
  }
}
