{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "type": "command",
        "powershell": "if ($env:COPILOT_PLUGIN_ROOT) { $s = Join-Path (Join-Path $env:COPILOT_PLUGIN_ROOT 'scripts') 'emit-contribution-boundary.ps1'; if (Test-Path $s) { & $s } else { '{}' } } else { '{}' }",
        "bash": "if [ -n \"${COPILOT_PLUGIN_ROOT:-}\" ]; then s=\"$COPILOT_PLUGIN_ROOT/scripts/emit-contribution-boundary.sh\"; if [ -f \"$s\" ]; then bash \"$s\"; else printf '{}'; fi; else printf '{}'; fi",
        "timeoutSec": 10
      }
    ]
  }
}
