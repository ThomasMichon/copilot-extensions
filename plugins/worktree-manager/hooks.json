{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "type": "command",
        "powershell": "if (-not (Get-Command worktree-manager -ErrorAction SilentlyContinue)) { Write-Host ''; Write-Host '[worktree-manager] Runtime not installed.' -ForegroundColor Yellow; Write-Host '  Ask Copilot to ''set up worktree-manager'' to bootstrap the runtime.' -ForegroundColor DarkGray; Write-Host '' }",
        "bash": "if ! command -v worktree-manager &>/dev/null; then echo ''; echo -e '\\033[33m[worktree-manager] Runtime not installed.\\033[0m'; echo -e '\\033[90m  Ask Copilot to set up worktree-manager to bootstrap the runtime.\\033[0m'; echo ''; fi",
        "timeoutSec": 5
      }
    ]
  }
}
